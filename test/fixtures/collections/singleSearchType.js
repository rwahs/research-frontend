(function () {
    'use strict';

    define(function () {
        return [
            {
                key: 'only',
                labelText: 'Only available search type',
                glyphicon: 'one'
            }
        ];
    });
}());
